services:
  - type: web
    name: mindprint-api
    runtime: node
    buildCommand: npx pnpm@9 install --frozen-lockfile && npx pnpm@9 --filter @mindprint/api build
    startCommand: node apps/api/dist/index.js
    envVars:
      - key: NODE_ENV
        value: production
    healthCheckPath: /v1/health
